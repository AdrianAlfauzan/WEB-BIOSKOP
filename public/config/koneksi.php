<?php
$namahost = "localhost"; // Ganti dengan nama host database Anda
$username = "root"; // Ganti dengan username database Anda
$password = ""; // Ganti dengan password database Anda
$database = "bioskop"; // Ganti dengan nama database Anda

// Membuat koneksi ke database
$koneksi = new mysqli($namahost, $username, $password, $database);


?>
